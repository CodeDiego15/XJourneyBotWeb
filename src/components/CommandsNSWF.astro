---
import Comman from "./Comman.astro"
---
<div class="px-4 grid flex-wrap gap-2 md:grid-cols-2 xl:grid-cols-4">
    <div class="bg-zinc-800 flex cursor-pointer flex-col overflow-hidden rounded-md px-4 py-2 text-left">
        <h3 class="text-white text-xl font-bold">/<strong class="text-pink-400">r34</strong> [tagName]</h3><span class="text-white mb-2 mt-0 text-left text-sm font-medium" style="white-space: pre-line;"> <strong class="text-white font-extrabold">Estado del comando:</strong><code class=" text-green-400"> Activo</code></span>
        <code class="max-w-[100%] p-0 text-left text-sm text-zinc-400">El comando /r34 [tagName] te permite buscar imágenes específicas en Rule 34 utilizando un tag particular.</code>
    </div>
    <div class="bg-zinc-800 flex cursor-pointer flex-col overflow-hidden rounded-md px-4 py-2 text-left">
        <h3 class="text-white text-xl font-bold">/<strong class="text-pink-400">hv</strong> [tagName]</h3><span class="text-white mb-2 mt-0 text-left text-sm font-medium" style="white-space: pre-line;"> <strong class="text-white font-extrabold">Estado del comando:</strong><code class=" text-red-600"> Inactivo</code></span>
        <code class="max-w-[100%] p-0 text-left text-sm text-zinc-400">El comando /hv [tagName] te permite buscar videos Hentai o Rule 34 específicas utilizando un tag particular.</code>
    </div>
    <div class="bg-zinc-800 flex cursor-pointer flex-col overflow-hidden rounded-md px-4 py-2 text-left">
        <h3 class="text-white text-xl font-bold">/<strong class="text-pink-400">cum</strong> @user.name</h3><span class="text-white mb-2 mt-0 text-left text-sm font-medium" style="white-space: pre-line;"> <strong class="text-white font-extrabold">Estado del comando:</strong><code class=" text-red-600"> Inactivo</code></span>
        <code class="max-w-[100%] p-0 text-left text-sm text-zinc-400">El comando /cum @user.name te permite hacerle cum a tu amig@</code>
    </div>
</div>

<Comman />